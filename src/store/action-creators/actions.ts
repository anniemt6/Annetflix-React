export const actions = {
    // not implemented
};

//TODO: Uncomment this line when actions will be created
/*
type ActionTypes<T> = T extends { [key: string]: infer U } ? U : never;
export type ActionType = ReturnType<ActionTypes<typeof actions>>;
 */

