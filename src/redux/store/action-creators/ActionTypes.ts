export const enum ResultActionType {
    SUCCESS = "SUCCESS",
    ERROR = "ERROR"
}

export const enum ResetActionType {
    RESET = 'RESET'
}

export const enum AuthActionType {
    SIGN_IN = 'SIGN_IN'
}